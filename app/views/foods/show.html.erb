<h1>詳細ページ</h1>
<%= link_to edit_food_path(@food.id) do %>
    <%= attachment_image_tag @food, :image, format: 'jpeg', size: "100x100" %>
<% end %>
<% @food.title %>
<% @food.expiration %>

<%= link_to '編集', edit_food_path(@food.id) %>
<%= link_to '一覧', root_path %>
<%= link_to '削除', food_path(@food), method: :delete, data: {confirm: "削除しますか？"} %>